apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cloudnativepg
resources:
  - namespace.yaml
  - release.yaml
configMapGenerator:
  - name: cloudnativepgapi-values 
    files:
      - values.yaml=values.yaml
configurations:
  - kustomizeconfig.yaml