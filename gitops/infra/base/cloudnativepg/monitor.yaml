apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    release: "prometheus"
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudnative-pg
      # release: "prometheus"
      # name:  postgres
  podMetricsEndpoints:
    - port: metrics