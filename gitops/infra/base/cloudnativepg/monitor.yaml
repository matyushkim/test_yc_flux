apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: controller-manager-metrics-monitor
spec:
  selector:
    matchLabels:
      Release: prometheus
  podMetricsEndpoints:
    - port: metrics