apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: grafana
commonLabels:
  app: grafana-dashboard-postges
configMapGenerator:
  - name: grafana-dashboard
    files:
      - grafana-dashboard.json
configurations:
  - kustomizeconfig.yaml