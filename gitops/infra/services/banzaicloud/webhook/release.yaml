apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: secrets-webhook
spec:
  interval: 10m
  chart:
    spec:
      chart: vault-secrets-webhook
      version: 1.19.0
      sourceRef:
        kind: HelmRepository
        name: banzaicloud
  install:
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
