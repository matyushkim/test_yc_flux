apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vault
  namespace: vault
  annotations: 
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: nginx
  rules:
  - host: vault.infra.matyshkinm.evrone.app
    http:
      paths:
      - path: /
        backend:
          service:
            name: vault
            port:
              number: 8200
        pathType: Prefix
  tls:
  - hosts:
    - vault.infra.matyshkinm.evrone.app
    secretName: matyshkin-com-vault-tls